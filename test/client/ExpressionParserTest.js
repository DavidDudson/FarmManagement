//import calculate from 'util/expression_parser.js'

var expect = require('expect.js');

describe('Expression Parser', function() {
    it('should do return empty string if parsed empty string', () =>  {
        expect(true).to.equal(true);
        //expect(calculate('')).to.equal('');
        //expect(calculate('   ')).to.equal('');
    });
});